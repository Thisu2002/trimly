{
  "name": "trimly",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "services/api",
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "docker compose up -d && cd services/api && npm run dev && cd prisma && npx prisma studio && cd ../web && npm run dev",
    "dev:mobile": "cd apps/mobile && npx expo start",
    "install:all": "npm install && cd apps/web && npm install && cd ../mobile && npm install"
  },
  "devDependencies": {
    "expo": "~54.0.31",
    "typescript": "^5"
  }
}
