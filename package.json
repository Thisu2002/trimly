{
  "name": "salonflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "docker compose up -d && cd apps/web && npm run dev && cd ../api && npm run dev && cd prisma && npx prisma studio",
    "dev:mobile": "cd apps/mobile && npx expo start",
    "install:all": "npm install && cd apps/web && npm install && cd ../mobile && npm install"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}